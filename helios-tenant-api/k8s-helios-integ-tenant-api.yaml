---
apiVersion: v1
kind: Service
metadata:
  name: tenant-api
  namespace: helios-integ
  labels:
    app: tenant-api
spec:
  selector:
    app: tenant-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-api-deployment
  namespace: helios-integ
  labels:
    app: tenant-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tenant-api
  template:
    metadata:
      labels:
        app: tenant-api
    spec:
      serviceAccountName: externalsecretsa
      containers:
      - name: tenant-api
        image: 590183943866.dkr.ecr.us-east-2.amazonaws.com/helios-integ-tenant-api:IMAGETAG
        ports:
        - containerPort: 5001
        resources:
          requests:
            memory: "0.5Gi"
            cpu: "0.5"
          limits:
            memory: "2Gi"
            cpu: "2"
        env:
          - name: ASPNETCORE_ENVIRONMENT
            value: Integ
