---
apiVersion: v1
kind: Service
metadata:
  name: user-api
  namespace: helios-dev
  labels:
    app: user-api
spec:
  selector:
    app: user-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001
  type: ClusterIP

---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: user-api-pdb
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: user-api

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-api-deployment
  namespace: helios-dev
  labels:
    app: user-api
spec:
  replicas: 4
  selector:
    matchLabels:
      app: user-api
  template:
    metadata:
      labels:
        app: user-api
      annotations:
        eks.amazonaws.com/compute-type: ec2
    spec:
      serviceAccountName: devsa
      containers:
      - name: user-api
        image: 529088296813.dkr.ecr.us-east-2.amazonaws.com/helios-dev-user-api-ecr:IMAGETAG
        ports:
        - containerPort: 5001
        resources:
          requests:
            memory: "8Gi"
            cpu: "4"
          limits:
            memory: "16Gi"
            cpu: "8"   
        env:
          - name: ASPNETCORE_ENVIRONMENT
            value: Newdev
---        
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-api-dev-scaling-hpa
  namespace: helios-dev
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user-api-deployment
  minReplicas: 4
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50    
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 50
