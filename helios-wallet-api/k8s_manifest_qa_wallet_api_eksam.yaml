apiVersion: v1
kind: Service
metadata:
  name: wallet-api
  namespace: helios-qa
  labels:
    app: wallet-api
spec:
  selector:
    app: wallet-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001

---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: wallet-api-pdb
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: wallet-api

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-api-deployment
  namespace: helios-qa
  labels:
    app: wallet-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wallet-api
  template:
    metadata:
      labels:
        app: wallet-api
      annotations:
        eks.amazonaws.com/compute-type: ec2
    spec:
      serviceAccountName: externalsecretsa
      containers:
      - name: wallet-api
        image: 375611915274.dkr.ecr.us-east-2.amazonaws.com/wallet-qa-api:IMAGETAG
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "0.5Gi"
            cpu: "0.5"
          limits:
            memory: "2Gi"
            cpu: "2"
        env:
          - name: ASPNETCORE_ENVIRONMENT
            value: Qa
